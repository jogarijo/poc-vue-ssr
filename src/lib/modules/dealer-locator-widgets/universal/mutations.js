export const setDealers = (state, dealers) => (state.dealers = dealers)
export const setSearch = (state, search) => (state.search = search)
export const setPage = (state, page) => (state.page = page)
export const setPerPage = (state, perPage) => (state.perPage = perPage)
